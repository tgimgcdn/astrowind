---
import Image from '~/components/common/Image.astro';

interface Props {
  items: {
    title: string;
    description: string;
    image: {
      src: string;
      alt: string;
    };
    href?: string;
  }[];
}

const { items } = Astro.props as Props;
---

<div class="relative">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 px-[400px] overflow-x-auto">
    {items.map((item) => (
      <a
        href={item.href || '#'}
        class="group block rounded-lg overflow-hidden shadow-lg hover:shadow-xl transition-shadow duration-300"
      >
        <div class="relative aspect-[4/3]">
          <Image
            class="w-full h-full object-cover"
            width={400}
            height={300}
            src={item.image.src}
            alt={item.image.alt}
            loading="lazy"
            decoding="async"
          />
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold mb-2 group-hover:text-primary transition-colors duration-300">
            {item.title}
          </h3>
          <p class="text-muted dark:text-slate-400">
            {item.description}
          </p>
        </div>
      </a>
    ))}
  </div>
</div> 
