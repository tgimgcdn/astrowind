---
import Layout from '~/layouts/Layout.astro';
import ContentGrid from '~/components/widgets/ContentGrid.astro';
import Header from '~/components/widgets/Header.astro';

// 创建16个内容项
const contentItems = Array(16).fill({
  title: '内容项',
  description: '这是一个内容项的描述，展示在网格布局中。',
  link: '#',
  image: 'https://source.unsplash.com/random/300x200?sig='
}).map((item, index) => ({
  ...item,
  title: `内容项 ${index + 1}`,
  image: `https://source.unsplash.com/random/300x200?sig=${index}`
}));

const navItems = [
  { text: 'Home', href: '/' },
  { text: 'Blog', href: '/blog' },
  { text: 'About', href: '/about' },
  { text: 'Contact', href: '/contact' },
];

const metadata = {
  title: '首页',
  description: '欢迎访问我们的网站',
};
---

<Layout metadata={metadata}>
  <div class="header-container px-[400px] mx-auto">
    <Header links={navItems} isSticky showToggleTheme position="right" />
  </div>
  <main class="flex-1">
    <section class="py-12 sm:py-16 lg:py-20">
      <div class="mx-auto max-w-7xl px-4 sm:px-6">
        <div class="mb-12 text-center">
          <h1 class="text-5xl md:text-6xl font-bold leading-tighter tracking-tighter mb-4 font-heading intersect-once intersect:fade-up">
            欢迎来到我们的网站
          </h1>
          <p class="text-xl text-gray-600 dark:text-slate-400 intersect-once intersect:fade-up intersect-delay-150">
            浏览我们精心准备的内容
          </p>
        </div>
        
        <ContentGrid 
          items={contentItems} 
          columns={4} 
          defaultStyles={true} 
          classes={{
            container: 'intersect-once',
            item: 'intersect-once'
          }}
        />
      </div>
    </section>
  </main>
</Layout>

<style>
  .header-container {
    width: 100%;
  }
  
  .fade-in {
    animation: fadeInAnimation 0.6s ease forwards;
    animation-delay: var(--animation-delay, 0ms);
  }

  @keyframes fadeInAnimation {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
